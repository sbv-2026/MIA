# ADR 0001: MIAAssistant sở hữu integration contract

Accepted. MIAAssistant là nguồn duy nhất của schemas, validators và test vectors. Host chỉ tiêu thụ artifact có version để tránh sửa lặp và contract drift.
